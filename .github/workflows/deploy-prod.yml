name: Deploy to PROD

## TODO TODO: Need to replace hardcoded 'tag' with pr tag number. 
## Can only verify this after we merge into dev which has PRs in it.

on:
  push:
    branches:
      # - dev  # Uncomment this if we want test deploys to also trigger prod deploys.
      - feat/ocp-prod # Remove this before accepting PR! We desire this only to happen when merging to dev.

concurrency:
  # Cancel in progress for PR open and close
  group: prod
  cancel-in-progress: false

jobs:


  # TODO - After switching to dev brand, verify this works in dev.
  # Currently can't test it in feat/ocp-prod branch, as no PRs there.
  get-pr-info:
    # Only run if the branch is 'dev'. If it's a feature branch like feat/ocp-prod, you MUST hardcode the tag below
    if: github.ref_name == 'dev'  
    runs-on: ubuntu-24.04
    outputs:
      pr: ${{ steps.vars.outputs.pr }}
    steps:
      - id: vars
        uses: bcgov-nr/action-get-pr@v0.0.1
      - name: Echo PR number
        run: |
          echo "PR: ${{ steps.vars.outputs.pr }}"

  builds:
    name: Builds
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        package: [backend, frontend, backend-migration]
        include:
          - package: backend
            build_file: ./backend/Dockerfile
            build_context: backend
          - package: backend-migration
            build_file: ./backend/Dockerfile.migrations
            build_context: backend
          - package: frontend
            build_file: ./frontend/Dockerfile
            build_context: frontend
    timeout-minutes: 10
    steps:
      - uses: bcgov-nr/action-builder-ghcr@v2.2.0
        with:
          package: ${{ matrix.package }}
          # Use input tag if provided, otherwise fallback to branch name (feat-ocp-prod -> feat-ocp-prod)
          tag: ${{ github.event.inputs.tag != '' && github.event.inputs.tag || github.ref_name }}
          tag_fallback: latest
          triggers: ('${{ matrix.package }}/')
          build_file: ${{ matrix.build_file }}
          build_context: ${{ matrix.build_context }}

  ## Prod Deploy
  ## Curently trigering on merge to dev, we can change if desired
  ## So now, test and prod are identical from a code/commit perspective (differ in envs, db params, etc)

## the db_prod job finishes before it's actually done
## as the helm install finishes fast, but then it waits a while for the operator to actually setup the stateful sets.
## so sometimes we comment out deploys_prod to JUST make sure db is deployed.
  deploy_db_prod:
    name: Deploys Database
    uses: ./.github/workflows/.dbdeployer.yml
    secrets: inherit
    with:
      environment: prod
      values: 'values-prod.yaml'
      app_values: 'values.yaml' # PROD env specific values file for the APP

      # Problem - if we need latest migration, we also need to change tag stuff.
      # this is using an old build, it's just deploying it.
  deploys_prod:
    name: Deploys to PROD
    uses: ./.github/workflows/.deployer.yml
    secrets: inherit
    needs: [deploy_db_prod, builds]
    with:
      environment: prod
      values: 'values-prod.yaml' # PROD env specific values file, will override values.yaml key/values but default to values.yaml
      # TODO: Replace hardcoded tag with '${{ needs.get-pr-info.outputs.pr }}'
      # tag: '286'
      # tag: '288'
      # same as the build tag above.
      tag: ${{ github.event.inputs.tag != '' && github.event.inputs.tag || github.ref_name }}
      # tag:  '${{ needs.get-pr-info.outputs.pr }}'




  # schemaspy:
  #   name: Schemaspy
  #   uses: ./.github/workflows/.schema-spy.yml
  #   secrets: inherit
  #   needs: [deploys]
  #   with:
  #     tag: prod
