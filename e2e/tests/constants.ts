// The imports in e2e tests are slightly different, but typescript still configured incorrectly.
// Just make sure it's set in the .env

//@ts-ignore
export const BASE_URL: string = process.env.BASE_URL;