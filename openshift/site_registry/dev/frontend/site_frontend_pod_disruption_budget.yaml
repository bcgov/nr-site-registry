apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: site-frontend
  namespace: c6a6e5-dev
  labels:
    app.kubernetes.io/name: site-frontend
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: site-frontend
  minAvailable: 2
