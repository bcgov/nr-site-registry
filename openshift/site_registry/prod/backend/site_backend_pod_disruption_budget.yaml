apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: site-backend
  namespace: c6a6e5-prod
  labels:
    app.kubernetes.io/name: site-backend
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: site-backend
  minAvailable: 2
