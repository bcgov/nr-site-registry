apiVersion: v1
kind: Service
metadata:
  name: site-postgres-cluster-bcgw
  namespace: c6a6e5-prod
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    postgres-operator.crunchydata.com/cluster: site-postgres-cluster
    postgres-operator.crunchydata.com/role: primary
